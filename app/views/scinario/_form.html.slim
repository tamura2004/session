= simple_form_for @scinario, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@scinario[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), scinarios_path, :class => 'btn btn-default'
